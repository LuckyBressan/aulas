function raiz(valor) {
    va = parseFloat(document.getElementById(valor).value);
    rai = Math.sqrt(va);
    return (rai);
}
function cubo(valor) {
    va = parseFloat(document.getElementById(valor).value);
    cub = Math.pow(va,3);
    return (cub);
}